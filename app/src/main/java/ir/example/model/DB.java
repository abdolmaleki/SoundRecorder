package ir.example.model;

import io.realm.Realm;

public class DB {
    Realm DB=Realm.getDefaultInstance();

    public static class Term{
        public void insert(TermModel model){

        }
    }
}
